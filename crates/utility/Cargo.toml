[package]
name = "bigospice-utility"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared SIMD data structures and data-oriented containers for BigOSpice"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
bigospice-core.workspace = true
smallvec.workspace = true
paste = "1"
serde = { version = "1", features = ["derive"], optional = true }
phf.workspace = true

[dev-dependencies]
