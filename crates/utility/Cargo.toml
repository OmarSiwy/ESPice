[package]
name = "pisim-utility"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared SIMD data structures and data-oriented containers for PiSIM"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
pisim-core.workspace = true
smallvec.workspace = true
paste = "1"
serde = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
