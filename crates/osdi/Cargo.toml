[package]
name = "bigospice-osdi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OSDI / OpenVAF compiled-model plugin loader for BigOSpice (LGPL-3, dlopen-only, no GPL linkage)"

[dependencies]
bigospice-core.workspace = true
bigospice-device.workspace = true
thiserror.workspace = true
libloading = "0.8"
libc = "0.2"

[dev-dependencies]
tempfile.workspace = true

[lib]
name = "bigospice_osdi"
path = "src/lib.rs"
